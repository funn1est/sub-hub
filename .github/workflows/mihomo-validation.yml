name: Mihomo validation

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  mihomo-validation:
    name: Official Mihomo ${{ matrix.version }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - version: v1.19.27
            sha256: fb3e34c55844f389ff54679e5a3aec331d5ec38006c20f8dcc476fb47768a58f
          - version: v1.19.29
            sha256: 60de76a35a6cbf7b4fa4a20f5c257c24345d1d635ab1aa3877022a1997ef413c
    env:
      CARGO_TERM_COLOR: always
      SUB_HUB_REQUIRE_MIHOMO: "1"
      SUB_HUB_REQUIRE_ACL4SSR_CORPUS: "1"
      SUB_HUB_MIHOMO_VERSION: ${{ matrix.version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Install Rust
        uses: jdx/mise-action@v4
        with:
          install_args: rust

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2

      - name: Checkout the pinned ACL4SSR corpus
        run: |
          set -eu
          commit='2fc7487be9ec0a0fcd7c91db319787d7b35a195d'
          corpus="${RUNNER_TEMP}/acl4ssr-corpus"
          git init "$corpus"
          git -C "$corpus" config core.autocrlf false
          git -C "$corpus" remote add origin https://github.com/ACL4SSR/ACL4SSR.git
          git -C "$corpus" fetch --depth=1 --no-tags origin "$commit"
          git -C "$corpus" checkout --detach FETCH_HEAD
          actual="$(git -C "$corpus" rev-parse --verify 'HEAD^{commit}')"
          test "$actual" = "$commit"
          echo "SUB_HUB_ACL4SSR_CORPUS_DIR=$corpus" >> "$GITHUB_ENV"

      - name: Download and verify Mihomo ${{ matrix.version }}
        run: |
          set -eu
          version='${{ matrix.version }}'
          asset="mihomo-linux-amd64-${version}.gz"
          expected='${{ matrix.sha256 }}'
          archive="${RUNNER_TEMP}/${asset}"
          extract="${RUNNER_TEMP}/mihomo-${version}"
          url="https://github.com/MetaCubeX/mihomo/releases/download/${version}/${asset}"
          curl -fsSL "$url" -o "$archive"
          echo "${expected}  ${archive}" | sha256sum -c -
          mkdir -p "$extract"
          gzip -dc "$archive" > "${extract}/mihomo"
          chmod +x "${extract}/mihomo"
          echo "SUB_HUB_MIHOMO_BIN=${extract}/mihomo" >> "$GITHUB_ENV"

      - name: Run corpus and official Mihomo acceptance tests
        run: >-
          cargo test --locked -p sub-hub-conversion
          --test acl4ssr_corpus
          --test mihomo_validation
