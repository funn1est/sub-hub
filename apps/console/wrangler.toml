# Git-connected Pages: set the dashboard root directory to apps/console
# so this file is the project config. Do not add account_id.
name = "sub-hub-console"
pages_build_output_dir = "./dist"
compatibility_date = "2026-08-13"
