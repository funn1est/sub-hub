{
  "name": "@sub-hub/worker",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "worker-build --release",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload",
    "test:host": "node --test --test-isolation=none tests/miniflare.test.mjs"
  },
  "devDependencies": {
    "miniflare": "4.20260730.0",
    "wrangler": "4.122.0"
  },
  "engines": {
    "node": ">=22"
  }
}
