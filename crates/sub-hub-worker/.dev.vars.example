# Crate-local copy of the repository-root button schema. The Deploy-to-Cloudflare
# prompt reads `.dev.vars.example` at the clone root. Do not copy this file to
# `.dev.vars` and do not put a real token here.
SUB_HUB_ACCESS_TOKEN=
