# Workers Static Assets. Git Builds can keep the default deploy command
# (`npx wrangler deploy`). Dashboard root directory: apps/console.
# Do not add account_id.
name = "sub-hub-console"
compatibility_date = "2026-08-13"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
